apiVersion: v1
kind: Service
metadata:
  name: hello-service
spec:
  selector:
    app: hello-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30000  # можно оставить для фиксированного порта
  type: NodePort